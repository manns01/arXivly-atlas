name: daily-arxiv

on:
  schedule:
    # Primary: 06:00 UTC (~11:30 IST), weekdays. The RSS channel pubDate claims
    # arXiv announces at 00:00 ET (04:00 UTC), but rss.arxiv.org's own feed
    # generation lags that -- 2026-09-10 and 2026-09-11 both only had fresh
    # content by ~05:40-05:49 UTC. 06:00 gives a small margin past both.
    - cron: "0 6 * * 1-5"
    # Backup: 06:30 UTC (~12:00 IST). Covers a primary run that GitHub delayed
    # past the feed or dropped entirely. fetch_arxiv is idempotent -- if the
    # primary already wrote data/raw/<date>.json this run just rebuilds from it.
    - cron: "30 6 * * 1-5"
  workflow_dispatch:
  # External punctual trigger. GitHub's own `schedule` is best-effort and has
  # been dropping this repo's runs, so an off-platform scheduler (cron-job.org)
  # POSTs to the repo `dispatches` API each weekday morning at 06:00 and 06:30
  # UTC (mirroring the schedule crons above):
  #   curl -X POST -H "Authorization: Bearer <fine-grained PAT, Contents: write>" \
  #        -H "Accept: application/vnd.github+json" \
  #        https://api.github.com/repos/manns01/arXivly-atlas/dispatches \
  #        -d '{"event_type":"daily-arxiv"}'
  # The schedule crons above stay as a fallback.
  repository_dispatch:
    types: [daily-arxiv]

# Don't let a slow run overlap the next one and race the git push.
concurrency:
  group: daily-arxiv
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write        # commit the day's data/raw/<date>.json back to main
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - run: pip install -r requirements.txt

      # fetch -> atlas -> generate. Non-zero exit fails the job and nothing
      # deploys, so the previously published site stays up.
      - run: python build.py

      - name: Commit new raw data
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/raw
          if git diff --cached --quiet; then
            echo "No new raw data to commit."
          else
            git commit -m "data: arXiv announcements $(date -u +%Y-%m-%d)"
            git push
          fi

      - uses: actions/upload-pages-artifact@v3
        with:
          path: docs/

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
